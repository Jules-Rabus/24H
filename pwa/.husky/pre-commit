# Run lint-staged (format + lint on staged files only)
docker compose exec -T pwa npx lint-staged

# Run full type check (cannot be scoped to staged files)
docker compose exec -T pwa npm run type:check
